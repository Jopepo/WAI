import SwiftUI

// Ads removed. This file is intentionally kept as a harmless placeholder so the
// project will not break if Xcode still references BannerAdView.swift.
struct BannerAdView: View {
    var body: some View {
        EmptyView()
    }
}
